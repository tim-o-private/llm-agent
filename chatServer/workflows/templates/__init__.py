"""Workflow template content and step prompts.

Templates are stored at {system_dir}/workflows/{name}.md on local disk.
Step prompts at {system_dir}/workflows/prompts/{template}/{step}.md.

This module holds the canonical content that gets seeded on startup.
"""
