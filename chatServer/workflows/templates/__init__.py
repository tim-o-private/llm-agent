"""Workflow template content and step prompts.

Templates are stored in ConfigService at system/workflows/{name}.md.
Step prompts at system/workflows/prompts/{template}/{step}.md.

This module holds the canonical content that gets seeded on startup.
"""
