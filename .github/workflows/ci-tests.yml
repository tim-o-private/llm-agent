name: CI Tests

on:
  pull_request:
    branches:
      - main
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x] # Test against common LTS versions

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
           version: 10 # Specify your pnpm version if needed

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Vitest tests
        run: pnpm test --verbose # Assuming 'pnpm test' executes 'vitest run' or similar CI-friendly command

      # Optional: If you want to generate and upload coverage reports
      # - name: Generate coverage report
      #   run: pnpm coverage # Assuming 'pnpm coverage' executes 'vitest run --coverage'

      # - name: Upload coverage reports to Codecov
      #   uses: codecov/codecov-action@v4
      #   with:
      #     token: ${{ secrets.CODECOV_TOKEN }} # only required for private repos
      #     fail_ci_if_error: true # optional (default = false)
      #     # You might need to specify the file path if it's not auto-detected
      #     # files: ./coverage/clover.xml,./coverage/lcov.info 