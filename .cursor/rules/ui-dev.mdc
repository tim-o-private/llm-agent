---
description: 
globs: ui-implementation-plan.md,*/clarity/*,*.tsx
alwaysApply: false
---
# MANDATORY: Memory Bank Compliance

**READ FIRST**: `memory-bank/README.md` - Navigate to your work area's patterns

**CRITICAL RULES**:
1. **Database**: PostgreSQL on Supabase ONLY - no SQLite, Redis, MongoDB
2. **API**: Use React Query hooks - avoid new endpoints  
3. **UI**: Radix Themes `<Theme>` + `@radix-ui/react-*` primitives
4. **Tools**: Generic CRUDTool configuration - no separate classes
5. **Connections**: Use `get_db_connection()` / `get_supabase_client()` only

**Pattern Files** (READ for your area):
- Frontend: `memory-bank/patterns/ui-patterns.md`
- Backend: `memory-bank/patterns/api-patterns.md`
- Agent: `memory-bank/patterns/agent-patterns.md`  
- Database: `memory-bank/patterns/data-patterns.md`

**Validation**: `node memory-bank/tools/link-checker.js`
